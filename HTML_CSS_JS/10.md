<p>The <code>len()</code> function returns the length of an object.</p>
Q2
<pre>
def factorial(n):
    if n == 0:
        return 1
    return n * factorial(n - 1)
</pre>
Q3
<p>Press <kbd>Ctrl + C</kbd> to copy the selected text.</p>
Q4
<p>The program output is <samp>Hello, World!</samp>.</p>
Q5
<p>The formula is F = <var>m</var>a.</p>
Q6
<style>
pre {
  background: #1e1e1e;
  color: white;
  padding: 15px;
  border-radius: 8px;
  font-family: monospace;
}
</style>

<pre>
for i in range(5):
    print(i)
</pre>
Q7
<p>Use the CSS property <code>display: flex;</code> for flexible layouts.</p>

<pre>
git add .
git commit -m "Initial commit"
git push origin main
</pre>
Q8
<p>Run <kbd>pwd</kbd> to see the current directory: <samp>/home/user/project</samp>.</p>
Q9
<style>
kbd {
  background: #f2f2f2;
  border: 1px solid #999;
  padding: 4px 8px;
  border-radius: 5px;
  font-family: monospace;
}
</style>

<p>Press <kbd>Enter</kbd> to continue.</p>
Q10
<p>Use <code>test -f filename</code> to check whether a file exists.</p>
Q11
<pre>
+---------+
|  Hello  |
|  World  |
+---------+
</pre>
Q12
<p>The variable <var>count</var> is assigned the value <code>count = 10</code>.</p>
Q13
<p>Use <code>display: flex;</code> for flexible layouts.</p>

<p>Press <kbd>Ctrl + S</kbd> to save the file.</p>

<p>The output is <samp>File saved successfully.</samp></p>
Q14
<style>
kbd {
  background: #f2f2f2;
  border: 1px solid #999;
  padding: 5px 8px;
  border-radius: 5px;
}

pre {
  background: #1e1e1e;
  color: white;
  padding: 15px;
  border-radius: 8px;
}
</style>

<p>Press <kbd>Ctrl + S</kbd> to save.</p>

<pre>
print("Hello World")
print("Welcome")
</pre>
Q15
<!DOCTYPE html>
<html>
<head>
  <title>HTML Code Elements</title>
  <style>
    pre {
      background: #1e1e1e;
      color: white;
      padding: 15px;
      border-radius: 8px;
    }

    kbd {
      background: #f2f2f2;
      border: 1px solid #999;
      padding: 4px 8px;
      border-radius: 5px;
    }
  </style>
</head>
<body>

<p>Use <code>color: red;</code> to change text color.</p>

<pre>
if (x &gt; 10) {
    console.log("Large");
}
</pre>

<p>Press <kbd>Ctrl + S</kbd> to save.</p>

<p>Error: <samp>File not found</samp></p>

<p>The value of <var>x</var> is 10.</p>

</body>
</html>
